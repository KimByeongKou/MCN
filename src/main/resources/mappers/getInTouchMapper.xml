<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="org.eney.mapper.GetInTouchMapper">

	<insert id="createGetInTouch">
		insert into tbl_get_in_touch(touch_name, touch_phone_num, touch_email, touch_info)
		values(#{touch_name}, #{touch_phone_num}, #{touch_email}, #{touch_info})
	</insert>

</mapper>